<% provide(:title, "Log in") %>
<div class="container">
<h1>Log in</h1>

<div>
  <div>
    <%= form_for(:session, url: '/sessions') do |f| %>

      <%= f.label :email %>
      <%= f.email_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.submit "Log in" %>
    <% end %>

    <p>Nova ONG? <%= link_to "Registre-se agora!", '/ongs/new' %></p>
  </div>
</div>  
</div>
